{
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
  "build": "bun install --cwd backend && bun install --cwd my-app && bun run build --cwd my-app",
  "start": "bun run --cwd backend start & bun run --cwd my-app bun run dev"
}
}